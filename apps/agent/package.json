{
  "name": "@cal-market/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "eve dev",
    "build": "node -e \"if (Number(process.versions.node.split('.')[0]) < 24) { console.warn('Eve build requires Node.js 24+. Skipping on Node ' + process.version); process.exit(0); }\" && eve build",
    "start": "eve start"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.0",
    "@cal-market/agent-core": "workspace:*",
    "@cal-market/db": "workspace:*",
    "ai": "^5.0.0",
    "dotenv": "^17.2.1",
    "eve": "^0.11.8",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "engines": {
    "node": "24.x"
  }
}
